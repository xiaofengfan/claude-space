# 多工作空间管理架构 — 知识沉淀

> 来源记忆：multi-workspace.md
> 沉淀时间：2026/6/28 00:52:28
> 类型：project

---

v1.1.5 引入了多工作空间管理。每个工作空间（workspace）拥有独立的工作目录和工作空间配置文件（`.claude-space/workspace.json`）。

核心数据流：
- `WorkspaceSelectDialog` → 用户选择/创建工作空间
- `main.ts` 中 `getWorkspaceRoot()` 返回当前活跃工作空间根目录
- 每个 workspace 维护独立的 session/terminal 进程池
- 切换 workspace 时，旧进程不会自动销毁（保留状态），但 UI 切换到新 workspace

**Why:** commit 58cbb21 `v1.1.5: 多工作空间管理 + Chat消息修复 + 设置Tab化优化`
**How to apply:** 新增功能需要感知当前 workspace — 文件操作使用 `getWorkspaceRoot()` 获取根路径，进程绑定到 `sessionProcesses` 时需记录所属 workspace。

---

## 核心要点

- 

## 应用场景

- 

## 相关参考

- 