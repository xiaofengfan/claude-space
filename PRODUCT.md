# Claude Space 产品说明书

> **版本**: v1.0.1 | **类型**: 桌面应用程序  
> **定位**: AI 驱动的项目开发工作台 — 多模型、多智能体、远程协作、自主演进

---

## 项目说明

本项目目前是基于 **Claude Code CLI** 的 Web UI 管理平台（第一阶段），为其提供图形化的项目管理、多智能体协作、任务看板、SSH 远程开发等增强功能。项目完全由 **Claude**、**DeepSeek** 等 AI 模型自动化开发。

**核心愿景**：打造一个打破项目边界、人员壁垒、技术差异、地域限制的**多边协同开发平台**——让不同项目、不同角色、不同技术栈、不同地区的开发者能够在一个统一的工作空间中无缝协作。虽然当前仅完成了第一阶段（本地单人工作台），但架构已为多用户协作、跨客户端通讯、云边协同预留了扩展能力。

后续将根据实际需要持续扩展与集成，逐步演进为本地 + 云平台协同的多模型系统开发中心。

---

## 一、产品概述

Claude Space 是一款基于 Electron + React 的本地桌面开发工作台，深度集成 Claude Code CLI，将 AI 编程助手从命令行升级为图形化的**多智能体协作平台**。它不仅是代码编辑器，更是一个**多边协同开发中心**——管理多个 AI 模型、编排智能体群聊、连接远程服务器、一键部署项目，为不同项目、不同角色、不同技术栈、不同地区的开发者提供统一协作空间。

### 核心理念

> 打破边界，协同创造——让不同项目、不同人员、不同技术、不同地区在统一平台上高效协作。

---

## 二、核心功能

### 2.1 🤖 多模型 Chat 交互

| 功能 | 说明 |
|------|------|
| 多模型支持 | Claude / DeepSeek / OpenAI / 自定义模型，统一界面切换 |
| 流式响应 | 实时渲染 AI 回复、thinking 折叠块、工具调用卡片 |
| 权限审批 | 敏感操作（Bash / Write / Edit）弹窗确认，支持自动审批 |
| 会话管理 | 多标签页会话，历史回放，持久化命名 |
| 终端模式 | 内置 xterm.js 终端，与 Chat 面板实时同步 |

### 2.2 👥 多智能体群聊（@all / @角色）

| 功能 | 说明 |
|------|------|
| 虚拟团队 | 8 人默认团队 + Claude：项目经理、产品经理、架构师、高级/开发工程师、测试、审查 |
| @mention 指派 | `@all` 全员通知，`@张架构` 定向指派，智能体并行回复 |
| 角色状态 | 实时反映工具调用对应的智能体忙碌/空闲状态 |
| 像素办公室 | 等距像素风虚拟工位，可视化团队工作状态 |
| 群聊持久化 | 群聊消息与任务关联，支持历史回溯 |

### 2.3 📊 任务与项目管理

| 功能 | 说明 |
|------|------|
| 任务看板 | todo / in_progress / done 三栏看板，拖拽管理 |
| 自动任务提取 | 从 Claude 会话 JSONL 历史中自动提取 TaskCreate / TaskUpdate 事件 |
| 任务监控 | 实时追踪工具调用进度，审批状态一览 |
| 项目扫描 | 自动发现工作区项目，读取 CLAUDE.md 提取描述和技术栈 |
| 文件浏览 | 递归文件树，支持代码高亮预览，Git 状态标记 |
| Git 集成 | status / log / diff / branch / commit / push / pull 内联操作 |

### 2.4 🌐 SSH 远程协作（v1.1 新增）

| 功能 | 说明 |
|------|------|
| SSH 服务器管理 | 密码/密钥双认证，主机指纹 TOFU 验证 |
| 远程终端 | 基于 xterm.js + ssh2 的交互式远程 shell |
| 远程文件浏览 | SFTP 递归目录树，文件读取/写入 |
| 一键部署 | 配置部署目标，SFTP 上传 + 前置/后置命令执行 |
| 安全设计 | 密码/密钥永不出 Electron 主进程，渲染进程仅接收脱敏 hint |

### 2.5 🎨 用户体验

| 功能 | 说明 |
|------|------|
| 暗色/亮色主题 | 一键切换，CSS 变量驱动 |
| 自定义标题栏 | 无框窗口 + 自绘标题栏 + 菜单栏 |
| 面板拖拽 | 左右侧栏可拖拽调整宽度 (180-560px) |
| 快捷键 | 完整菜单快捷键体系 (Ctrl+N/O/1-8/Shift+N 等) |

---

## 三、技术架构

```
┌─────────────────────────────────────────────────┐
│                  Electron 28                     │
│  ┌──────────────┐  ┌──────────────────────────┐ │
│  │  渲染进程     │  │       主进程 (Node.js)    │ │
│  │              │  │                          │ │
│  │ React 18     │  │ Claude CLI spawn         │ │
│  │ TypeScript   │◄─┤ AgentPool (多智能体)      │ │
│  │ xterm.js     │  │ TerminalProcess (PTY)    │ │
│  │ Vite 5       │  │ SshService (ssh2+SFTP)   │ │
│  │              │  │ ConnectionService        │ │
│  └──────────────┘  └──────────────────────────┘ │
│         ↕ contextBridge (preload.ts)             │
├─────────────────────────────────────────────────┤
│  外部依赖: Claude CLI | ssh2 | node-pty | Git    │
└─────────────────────────────────────────────────┘
```

| 层 | 技术选型 |
|----|----------|
| 桌面壳 | Electron 28 + contextBridge 安全隔离 |
| 前端 | React 18 + TypeScript + Vite 5 + CSS Variables |
| 终端 | xterm.js + node-pty (本地) + ssh2 (远程) |
| AI 集成 | Claude CLI stream-json 双向通信 |
| 任务 | useTaskSync hook 实时事件→任务映射 |
| 存储 | JSON 文件 (~/.claude/*.json) |

---

## 四、产品优势

### 4.1 对比传统 IDE + AI 插件

| 维度 | 传统 IDE + Copilot | Claude Space |
|------|-------------------|--------------|
| AI 交互 | 行内补全，单轮对话 | 多智能体群聊，上下文传递 |
| 任务管理 | 需外部工具 (Jira/Trello) | 内置看板 + 自动提取 |
| 终端 | 独立窗口 | Chat + 终端实时同步 |
| 远程开发 | 需 VSCode Remote | 内置 SSH + SFTP + 部署 |
| 团队协作 | 依赖 Git 平台 | 虚拟团队 + 智能体指派 |

### 4.2 对比 Claude Code CLI

| 维度 | Claude Code CLI | Claude Space |
|------|----------------|--------------|
| 界面 | 纯文本终端 | 富 UI + 工具调用卡片 |
| 多任务 | 单进程 | 多会话池 + 并行智能体 |
| 可见性 | 命令行输出 | thinking 折叠 + 进度条 + 任务看板 |
| 部署 | ❌ | ✅ SFTP 一键部署 |

### 4.3 安全特性

- **零信任渲染进程**：API Key / SSH 密码仅在主进程明文存在，渲染进程收到脱敏 hint
- **contextBridge 隔离**：渲染进程无 Node.js 权限
- **主机指纹验证**：SSH TOFU (Trust-On-First-Use) 防中间人攻击
- **部署排除**：自动跳过 .env / .git / node_modules

---

## 五、发展路线图

### Phase 1: 已完成 (v1.0.x)

- [x] 多模型 Chat + 流式响应 + 权限审批
- [x] 多智能体群聊 + 像素办公室
- [x] 任务看板 + 自动提取 + 监控
- [x] 本地终端 + Chat 同步
- [x] Git 集成
- [x] SSH 远程终端 + 文件浏览 + 部署

### Phase 2: Claw 助手自动化开发 (v1.2)

> 核心目标：从"人指挥 AI"升级为"AI 自主工作"

| 功能 | 描述 |
|------|------|
| 🦞 Claw 自动化引擎 | 基于事件驱动的自主开发循环：监听 Git → 分析需求 → 编码 → 测试 → 提交 PR |
| 📋 需求解析器 | 从 Issue / PR / 自然语言自动生成开发任务 |
| 🔄 CI/CD 集成 | 自动触发构建、测试、部署流水线 |
| 🧪 自主测试 | AI 自动生成测试用例，运行并修复失败测试 |
| 📊 开发看板 | 自动化任务进度可视化，Token 消耗统计 |

### Phase 3: 本地 + 云平台多边协作 (v1.3)

> 核心目标：打通本地开发环境与云平台，实现多端协同

| 功能 | 描述 |
|------|------|
| ☁️ 云平台连接器 | 对接 GitHub / GitLab / Gitee，双向同步项目与任务 |
| 🔗 多工作区管理 | 同时管理本地项目 + 远程服务器项目 + 云端仓库 |
| 📡 边云协同 | 本地编辑 → 云端构建 → 远程部署，全链路自动化 |
| 🗂️ 统一项目视图 | 聚合本地 + SSH + Git 仓库的项目资源管理器 |
| 🔐 凭证管理 | 统一管理 Git / SSH / API 凭证，主进程加密存储 |

### Phase 4: 多用户协作与同步 (v1.4)

> 核心目标：从单人工作台升级为团队协作平台

| 功能 | 描述 |
|------|------|
| 👤 多用户账号 | 本地用户切换，角色权限管理 |
| 🔄 实时同步 | 项目设置、任务看板、团队配置跨设备同步 |
| 💬 团队消息 | 用户间消息通讯，任务评论与讨论 |
| 📋 共享看板 | 多人实时编辑任务看板，状态变更推送 |
| 🔒 权限模型 | 所有者 / 编辑者 / 查看者三级权限 |

### Phase 5: 跨客户端消息通讯 (v1.5)

> 核心目标：打通桌面端与移动端，随时随地掌控开发进度

| 功能 | 描述 |
|------|------|
| 🖥️↔📱 跨端通讯 | WebSocket / WebRTC 实现桌面端与 Web 端实时消息 |
| 🔔 推送通知 | 任务完成、构建结果、审批请求推送到移动端 |
| 📱 Web 客户端 | 基于同一套 xterm.js + React 组件的浏览器版本 |
| 🔗 消息中继 | 本地服务作为中继，支持局域网内多设备协作 |

### 愿景: 自主演进的多模型系统开发中心

```
                      ┌──────────────────────┐
                      │   Claude Space Hub    │
                      │   云边协同指挥中心      │
                      └──────┬───────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   ┌────▼─────┐      ┌──────▼──────┐      ┌─────▼─────┐
   │ 本地节点  │      │  远程节点    │      │ 云端节点   │
   │ Electron │◄────►│ SSH Server  │◄────►│ K8s Pod   │
   │ Windows  │      │ Linux       │      │ Container │
   └──────────┘      └─────────────┘      └───────────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                    ┌────────▼────────┐
                    │  数字化公司 OS   │
                    │                 │
                    │  🦞 Claw 引擎   │ ← 自主开发循环
                    │  🤖 智能体团队   │ ← 角色分工协作
                    │  📊 项目管理     │ ← 需求→任务→交付
                    │  🚀 CI/CD       │ ← 自动构建部署
                    │  📡 云边协同     │ ← 多端资源调度
                    └─────────────────┘
```

**终极目标**：将 Claude Space 打造为**个人/团队的数字化公司操作系统**——一个自主、可交互、持续演进的多模型系统开发中心。AI 不再是工具，而是 24x7 工作的数字员工团队。

---

## 六、系统要求

| 项目 | 最低配置 |
|------|----------|
| OS | Windows 10+ / macOS 12+ / Linux |
| Node.js | 18+ |
| Claude CLI | `@anthropic-ai/claude-code` |
| 内存 | 4 GB+ |
| 磁盘 | 200 MB |

---

## 七、快速开始

```bash
git clone <repo-url>
cd claude-space
npm install
npm run dev          # 开发模式
npm run electron:build  # 打包安装程序
```

设置环境变量 `ANTHROPIC_API_KEY` 后启动即可使用。
